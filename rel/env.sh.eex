#!/bin/sh

# Configure the release node. Sets a unique distribution node name and cookie
# for `bin/vigil start`, and turns the Phoenix endpoint server on so the
# release actually serves HTTP (eval/rpc commands leave it off).

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE="vigil@127.0.0.1"
export PHX_SERVER=true
